# Phase 4 Switching And Extensibility Requirements Questions

Purpose: refine the next layer of the Phase 4 scaffold so it can become a
comfortable, stable, and extensible mini training framework rather than a
single-run script.

Reply format suggestion:

```text
1B; 2C; 3A; ...
```

If a choice needs nuance, add a short note after the option list.

---

## A. Product Positioning

1. How should we position cache, mixed precision, batch-size sweep, and dataloader tuning?
   - A. Optional experiments only
   - B. Basic framework capabilities exposed by config, but not guaranteed speedups (recommended)
   - C. Mandatory optimizations always enabled
   - D. Report-only discussion, not framework features

2. What should "comfortable switching" mean for Phase 4?
   - A. Only switch among existing configs manually
   - B. Config-level switching for GPT-style causal LM and text datasets (recommended)
   - C. Fully seamless switching for any HuggingFace architecture
   - D. Full automatic adaptation including OOM retry and code patching

3. What is the correct ambition for model/data extensibility?
   - A. Minimal: keep current TinyGPT/DistilGPT2/TinyStories only
   - B. Practical: support a few well-tested model/data profiles (recommended)
   - C. Broad: support many model families with clear preflight warnings
   - D. Aggressive: attempt DeepSeek/Qwen/LLaMA family as first-class targets now

4. What should the final report emphasize after this extension?
   - A. The framework can train one model successfully
   - B. The framework can compare optimization toggles on one setup
   - C. The framework can switch model/data profiles and explain capability limits (recommended)
   - D. The framework can train a large model competitively

---

## B. Model Switching Scope

5. Which model families should be first-class supported now?
   - A. `sshleifer/tiny-gpt2` and `distilgpt2` only
   - B. GPT-style causal LM: tiny-gpt2, distilgpt2, gpt2 (recommended)
   - C. B plus Qwen small model
   - D. B plus Qwen/DeepSeek/LLaMA-family models

6. Should `gpt2` be added as a real stress config?
   - A. No, keep it report-only
   - B. Yes, add config but do not run unless time permits (recommended)
   - C. Yes, run one short stress experiment now
   - D. Yes, make it the new main baseline

7. Should Qwen/DeepSeek be attempted in this Phase 4 project?
   - A. No, too risky for current scope
   - B. Add profile placeholders and preflight notes only (recommended)
   - C. Add one Qwen small smoke config if downloads/memory allow
   - D. Try DeepSeek-family model as a stretch run

8. How should pretrained weights be handled for larger models?
   - A. Always train from config only
   - B. Keep both `from_config` and `from_pretrained`, default `from_config` (recommended)
   - C. Use pretrained weights for all non-debug runs
   - D. Agent decides based on model size

9. Should the framework support model profile files?
   - A. No, keep full configs only
   - B. Yes: `configs/model_profiles/*.yaml` (recommended)
   - C. Yes, but as Python files
   - D. Yes, with a Hydra-style config system

10. What should a model profile include?
   - A. Only `name_or_path`
   - B. model name, tokenizer name, trust_remote_code, from_pretrained, gradient checkpointing (recommended)
   - C. B plus recommended batch/seq_len
   - D. C plus expected memory and known caveats

11. Should the framework estimate parameter count before training?
   - A. No, current runtime summary is enough
   - B. Yes, after model construction only (current behavior)
   - C. Yes, and write it to preflight report (recommended)
   - D. Yes, and use it to automatically change batch size

12. How should unsupported model architectures fail?
   - A. Let HuggingFace errors surface directly
   - B. Catch common failures and print guidance (recommended)
   - C. Auto-switch to a fallback model
   - D. Agent attempts source-code patching automatically

---

## C. Dataset Switching Scope

13. Which datasets should be first-class now?
   - A. local fixture and TinyStories only
   - B. local fixture, TinyStories, WikiText-2 (recommended)
   - C. B plus OpenWebText subset
   - D. Arbitrary HuggingFace dataset through config only

14. Should the framework support data profile files?
   - A. No, keep full configs only
   - B. Yes: `configs/data_profiles/*.yaml` (recommended)
   - C. Yes, but generated by agent only
   - D. Use command-line flags instead

15. What should a data profile include?
   - A. dataset_name and text_field only
   - B. dataset_name, config, split, text_field, max_samples, validation split (recommended)
   - C. B plus cache policy and dataloader worker policy
   - D. C plus known expected token counts

16. How should non-standard text fields be handled?
   - A. User manually edits `text_field`
   - B. Preflight checks whether `text_field` exists and gives examples (recommended)
   - C. Agent scans dataset columns and auto-picks one
   - D. Support multi-column text templates now

17. Should sequence packing stay simple fixed blocks?
   - A. Yes, fixed token blocks only (recommended)
   - B. Add variable-length examples with padding
   - C. Add dynamic packing for better efficiency
   - D. Support both fixed and dynamic packing now

18. Should cache key include more environment information?
   - A. Current dataset/tokenizer/seq_len/max_samples is enough
   - B. Include data profile name and framework cache version (recommended)
   - C. Include Python/transformers version too
   - D. Include full git hash and environment hash

---

## D. Config And Profile System

19. Should we add config composition?
   - A. No, keep one full YAML per experiment
   - B. Add simple `--base --model-profile --data-profile --override` merge (recommended)
   - C. Adopt Hydra/OmegaConf
   - D. Let the agent generate full configs only

20. What should the default user experience be?
   - A. `python train.py --config configs/debug.yaml`
   - B. A plus optional profile composition command (recommended)
   - C. Only agent commands
   - D. Shell scripts only

21. Should profile composition write the resolved config into the run directory?
   - A. No
   - B. Yes, always write the fully resolved config (recommended)
   - C. Yes, plus the original component profiles
   - D. Yes, plus config diff against baseline

22. How strict should config validation be?
   - A. Minimal validation
   - B. Fail on unknown keys and invalid core values (current/recommended)
   - C. B plus compatibility checks between model/data/trainer
   - D. C plus automatic fixes

23. Should configs include a `tags` or `notes` field?
   - A. No
   - B. Yes, useful for ledger/report grouping (recommended)
   - C. Only agent-generated notes
   - D. Add full experiment metadata schema now

---

## E. Optimization Capabilities

24. Which optimization toggles should be basic config capabilities?
   - A. cache only
   - B. cache, mixed precision, grad accumulation, grad clipping (recommended)
   - C. B plus dataloader workers/pin memory/persistent workers
   - D. C plus torch.compile and activation checkpointing

25. Should mixed precision be enabled automatically?
   - A. No, off by default
   - B. `auto` by config, but report exact dtype and whether CUDA supports bf16 (recommended)
   - C. Always on for CUDA
   - D. Agent decides per run

26. Should cache be enabled by default for real datasets?
   - A. No, require explicit cache config
   - B. Yes, enabled by default for real datasets, but compare cold/hit in experiments (recommended)
   - C. Always cache all datasets
   - D. Agent decides after first run

27. Should dataloader worker tuning be automatic?
   - A. No, manual config
   - B. Add a small sweep option for `num_workers=0/2` (recommended)
   - C. Agent automatically sets workers from timing
   - D. Full dataloader autotuning

28. Should batch-size / grad-accumulation sweep become a first-class experiment?
   - A. No
   - B. Yes, because current timing suggests optimizer/per-step overhead (recommended)
   - C. Yes, but only after adding gpt2
   - D. Agent should always run it after baseline

29. Should activation checkpointing be implemented now?
   - A. No, defer
   - B. Keep optional model config toggle only (current/recommended)
   - C. Add one experiment if using gpt2/Qwen
   - D. Enable automatically on memory pressure

30. Should `torch.compile` be implemented now?
   - A. No, defer
   - B. Keep existing `compile_model` config but off by default (recommended)
   - C. Add one experimental config
   - D. Enable for all CUDA runs

---

## F. Preflight, Safety, And Fallback

31. Should every run start with a preflight report?
   - A. No
   - B. Yes, lightweight preflight printed/logged before training (recommended)
   - C. Yes, and block training unless all checks pass
   - D. Yes, and auto-fix issues

32. What should preflight check?
   - A. Imports only
   - B. imports, device, tokenizer pad/eos, dataset text field, seq_len/batch config (recommended)
   - C. B plus GPU memory estimate
   - D. C plus download/cache availability

33. How should OOM be handled?
   - A. Let training fail
   - B. Catch CUDA OOM and print recommended smaller batch/seq_len (recommended)
   - C. Auto-retry with smaller batch
   - D. Agent runs an OOM recovery plan automatically

34. Should dependency installation be part of the framework?
   - A. No, document only
   - B. Keep `selfcmd install-deps` pinned for course server (recommended)
   - C. Add local virtualenv management
   - D. Full environment bootstrap

35. Should final workspace be cleaned before submission?
   - A. No, leave all prior phase files
   - B. Keep current mixed `/workspace`, but ensure `/workspace/report4.md` is correct
   - C. Add a Phase4-only clean final deploy mode that preserves only Phase4 files and evidence (recommended)
   - D. Submit from a separate directory if endpoint supports it

---

## G. Agent Behavior

36. Should the agent run experiment matrices?
   - A. No, one config at a time
   - B. Yes, small matrix from a YAML plan (recommended)
   - C. Yes, dynamic matrix based on previous results
   - D. Full AutoML-style search

37. What should the first matrix be?
   - A. cache on/off
   - B. batch size / grad accumulation sweep (recommended)
   - C. mixed precision on/off
   - D. model family sweep

38. Should the agent modify source code?
   - A. No, propose only
   - B. Write patch proposals with risk/rollback (current/recommended)
   - C. Apply patches to allowed files after tests
   - D. Fully autonomous source editing

39. Should the agent compare runs and name a "best" config?
   - A. No
   - B. Yes, based on tokens/sec plus validation loss sanity (recommended)
   - C. Yes, based only on tokens/sec
   - D. Yes, using a weighted score

40. Should the agent write report snippets automatically?
   - A. No
   - B. Yes, generate evidence-table snippets and short observations (recommended)
   - C. Yes, write entire report sections
   - D. Yes, rewrite `report4.md` automatically after every run

---

## H. Evidence And Report Impact

41. What evidence should prove "comfortable switching"?
   - A. Config files only
   - B. One successful switch: debug -> TinyStories/DistilGPT2
   - C. Two successful switches: GPT-style model profile plus dataset profile (recommended)
   - D. Three model families including Qwen/DeepSeek

42. Should we include failed/neutral optimization results in the report?
   - A. No, show only improvements
   - B. Yes, neutral results are important evidence of profiling understanding (recommended)
   - C. Yes, but keep them short
   - D. Only in appendix

43. Should the final report call this a "general framework"?
   - A. Yes, broad general framework
   - B. No, call it a causal-LM mini framework with extensible profiles (recommended)
   - C. Call it an agent-generated training framework only
   - D. Call it a prototype harness

44. How much should we discuss DeepSeek/Qwen if not run?
   - A. Do not mention
   - B. Mention as stretch direction and explain why deferred (recommended)
   - C. Include detailed speculative plan
   - D. Add profile placeholders and discuss preflight caveats

45. What is the most important next deliverable?
   - A. Add gpt2/DeepSeek configs immediately
   - B. Add profile composition + preflight + batch/grad sweep (recommended)
   - C. Polish report only
   - D. Submit current version first, then extend
